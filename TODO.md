1. Fix price scraping and add normalization for ingredients. Pull prices from existing receipts. (Allow user opt-in for sharing of prices. ONLY line item, price, and store will be shared.)
2. ~~Add Currency selection (searchable text box, required per profile)~~ DONE - currency picker in Profile (web + app)
3. Allow currency to be changed for line items/full receipts
4. Pull exchange rate data if requested by user (idk if this is needed)
5. Add end user usage instructions
